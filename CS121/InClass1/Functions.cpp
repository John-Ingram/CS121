//
// Functions
//

#include <iostream>
#include <fstream>
#include <iomanip>
#include <string>

using namespace std;

float convert(float temperature);

int main(void)
{
	float celsius = 0, fahr = 0;
	//enter temp in C
	fahr = convert(temp.c);
	system(pause);
	return 0;
}
float convert(float temperature) 
{
	float F = 0;
	F = 9.0;//math
	return F;
}
