//
// Program 1
//

#include <iostream>
#include <fstream>
#include <iomanip>
#include <string>

using namespace std;

int main(void)
{
	float x = 0, y = 0, r = 0, s = 0, t = 0;
	//1a
	r = 3 * (x * x) + 5 * x + 1;
	printf("R = " + r);
	return 0;
}