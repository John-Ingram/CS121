//
// ointers
//

#include <iostream>
#include <fstream>
#include <iomanip>
#include <string>

using namespace std;


int main(void)
{
	enum Days{SUN, MON, TUES, WED, THUR, FRI, SAT};
    unsigned int day = 0;
    Days today;
    today = MON;
    for(day = SUN; day <= SAT; day++)
    {
        cout<<"Enter high and low temps";
    }
	system("pause");
    return 0;
}

